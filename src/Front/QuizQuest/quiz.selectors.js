export const userName = (state) => state.quiz.name;
export const userPhone = (state) => state.quiz.phone;
export const userScore = (state) => state.quiz.score;
export const isShowingResults = (state) => state.quiz.showingResults;
